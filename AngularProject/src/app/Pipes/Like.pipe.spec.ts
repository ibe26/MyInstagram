/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LikePipe } from './Like.pipe';

describe('Pipe: Likee', () => {
  it('create an instance', () => {
    let pipe = new LikePipe();
    expect(pipe).toBeTruthy();
  });
});
